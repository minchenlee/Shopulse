version: '3'

services:
  backend:
    image: leeminchen/shopulse-backend
    restart: always
    networks:
      - shopulse
  nginx:
    image: nginx
    restart: always
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - "80:80"
    networks:
      - shopulse

networks:
  shopulse:
    driver: bridge
